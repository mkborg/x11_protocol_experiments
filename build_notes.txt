There are two possible build modes:
* local (or embedded) -- each subcomponent is built in its' own build subdirectory next to own source code
* global -- all subcomponents are built in common dedicated directory somewhere relatively far away from source code
